.DS_Store
.AppleDouble
.LSOverride
.idea/
.vscode/
.claude/

# Docker dev data（MySQL / Redis 宿主挂载目录，仅项目根的 /data/）
/data/


# Environment files（两份独立 .env，内含敏感信息，均不入库）
.env
backend/.env

# Agent 生成的发布与实例身份控制文件（随部署变化，不属于源码）
/.mallbase-deployment.json
/.mallbase-release-inventory.json
/.mallbase-sealed-context.json

# 部署脚本的本地配置（含真实主机 / 私钥 / 远程目录，不入库）
deploy/upload-frontend.local.sh

# 工作过程产生的临时目录（截图中转、调试输出等）
.tmp/


.playwright-cli/
output/
.superpowers/

# 内部方案草稿，不作为公开文档提交
docs/superpowers/
EXTENSION_SLOTS_DISTRIBUTION_PHASE1_PLAN.md
