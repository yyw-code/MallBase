.DS_Store
.AppleDouble
.LSOverride
.idea/
.vscode/
.claude/

# Docker dev data（MySQL / Redis 宿主挂载目录，仅项目根的 /data/）
/data/

# Frontend build artifacts
backend/public/admin/

# Environment files（两份独立 .env，内含敏感信息，均不入库）
.env
backend/.env

# Docker dev generated install state
deploy/install/install.lock
