# ============================================================
# backend/.example.env —— ThinkPHP 运行时模板
# ============================================================
# 用途：ThinkPHP 运行时通过 env 函数读取的完整业务配置，约 30 字段。
#      本文件是 backend/.env 的模板：
#        1. Docker 开发全套模式：ensure-env 会自动派生 backend/.env
#        2. 非 Docker 全套模式：也可手动 cp backend/.example.env backend/.env 再按需修改
#
# Docker 开发全套模式下，请优先修改项目根目录 .env，而不是直接编辑 backend/.env。
# Docker compose 变量插值模板在 deploy/docker/.example.env；详见 docs/env-files.md。
# ============================================================

# -------- 应用 --------
# 调试模式（生产必须设为 false）
APP_DEBUG=true
# 是否开启定时任务
CRON_ENABLE=false
# 队列连接：单机可用 sync；多副本/K8s 建议 redis，并单独运行 queue worker
QUEUE_CONNECTION=sync
# 是否启用 Swoole 内置队列 Worker；默认关闭，常规队列仍建议单独运行 php think queue:work
SWOOLE_QUEUE_ENABLE=false
QUEUE_REDIS_QUEUE=default
QUEUE_REDIS_HOST=redis
QUEUE_REDIS_PORT=6379
QUEUE_REDIS_PASSWORD=
QUEUE_REDIS_SELECT=0
QUEUE_REDIS_TIMEOUT=0
QUEUE_REDIS_PERSISTENT=false
# 默认语言
DEFAULT_LANG=zh-cn

# -------- 数据库 --------
DB_TYPE=mysql
# 连接目标：用 compose mysql 容器填 mysql；连宿主 MySQL 填 host.docker.internal；本地 swoole 直跑填 127.0.0.1
DB_HOST=mysql
# Docker 开发全套模式下由根 .env 同步
DB_NAME=mallbase
# Docker 开发全套模式下由根 .env 同步；不要使用 root
DB_USER=mallbase
# Docker 开发全套模式下由根 .env 同步
DB_PASS=please-change-or-leave-for-random
# ThinkPHP 连 MySQL 的容器内端口，固定 3306；不是宿主暴露端口（那个是 MYSQL_PORT）
DB_PORT=3306
DB_CHARSET=utf8mb4
DB_PREFIX=mb_

# -------- 缓存（默认 redis；本地无 redis 可改为 file） --------
CACHE_DRIVER=redis
CACHE_PREFIX=
CACHE_EXPIRE=0
CACHE_TAG_PREFIX=tag:

# -------- Redis --------
# Redis 主机：用 compose redis 容器填 redis；连宿主 Redis 填 host.docker.internal；本地直跑填 127.0.0.1
REDIS_HOST=redis
# ThinkPHP 连 Redis 的端口（cache.php / swoole.php 都读它）
# - compose 部署：必须保持 6379（redis 容器内监听 6379）；想改宿主暴露端口去根 .env 改 REDIS_PORT
# - 本地直跑：改成宿主 Redis 的实际端口
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_TIMEOUT=0
REDIS_PERSISTENT=false
REDIS_CACHE_DB=0

# -------- JWT（Docker 开发全套模式首次派生时会自动生成随机值） --------
JWT_SECRET=please-change-or-leave-for-random
JWT_EXPIRE=7200
JWT_REFRESH_EXPIRE=2592000

# -------- Swoole HTTP/Worker --------
SWOOLE_HTTP_HOST=0.0.0.0
# Docker 开发全套模式下由根 .env 同步
SWOOLE_HTTP_PORT=8080
# Docker 开发全套模式默认由根 .env 同步为 1，避免开发容器按 CPU 核数起过多 worker
SWOOLE_WORKER_NUM=0
SWOOLE_MAX_REQUEST=2000
SWOOLE_RELOAD_ASYNC=true
SWOOLE_MAX_WAIT_TIME=60
SWOOLE_HEARTBEAT_IDLE_TIME=120
SWOOLE_HEARTBEAT_CHECK_INTERVAL=60
SWOOLE_POOL_MAX_WAIT_TIME=5
SWOOLE_DB_POOL_MAX_ACTIVE=3
SWOOLE_CACHE_POOL_MAX_ACTIVE=3
SWOOLE_REDIS_POOL_MAX_ACTIVE=3

# -------- 站点域名 --------
# 运行期权威来源：mb_setting.site_url（安装时写入，后台可改）。
# SITE_URL 仅用于：
#   - Web 安装页表单默认值；未填写时回退当前 request 域名
#   - CLI 无人值守安装 install:auto 的输入来源
#   - 安装完成后写回此字段作为静态副本，便于运维查看当前站点域名
# Docker 开发全套模式下，此键会从根 .env 同步过来。
#
# CORS 不再通过环境变量配置。默认反射请求 Origin 头并允许 Credentials，
# 如需收紧策略请直接修改 backend/app/middleware/CorsMiddleware.php。
SITE_URL=
