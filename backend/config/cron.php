<?php

return [

    // 是否启用 Cron
    'enable' => env('CRON_ENABLE', false),

    // 只允许哪个 worker 启动
    'only_worker_id' => 0,


    /*
     |--------------------------------------------------------------------------
     | 定时任务类
     |--------------------------------------------------------------------------
     | 这里只放「任务类名」
     */

    'tasks' => [
        app\cron\tasks\TestCron::class,
    ],
];